
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Slideshow</title>
  <style>
    /* Styles for the slideshow container */
    .slideshow {
      overflow: hidden;
      position: relative;
      width: 800px;
      height: 400px;
      margin: 20px auto;
    }

    /* Styles for the slide images */
    .slideshow img {
      width: 100%;
      height: 100%;
      display: block;
      position: absolute;
      top: 0;
      left: 0;
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
    }

    /* Styles for the active slide */
    .slideshow img.active {
      opacity: 1;
    }

    /* Styles for the navigation dots (optional) */
    .dot {
      cursor: pointer;
      height: 15px;
      width: 15px;
      margin: 0 2px;
      background-color: #bbb;
      border-radius: 50%;
      display: inline-block;
      transition: background-color 0.6s ease;
    }

    .active .dot {
      background-color: #717171;
    }
  </style>
</head>
<body>
  <div class="slideshow">
    <img src="image1.jpg" alt="Image 1" class="active">
    <img src="image2.jpg" alt="Image 2">
    <img src="images3.jpg" alt="Image 3">
  </div>

  <div class="dots">
    <span class="dot" onclick="currentSlide(1)"></span>
    <span class="dot" onclick="currentSlide(2)"></span>
    <span class="dot" onclick="currentSlide(3)"></span>
  </div>

  <script>
    let slideIndex = 1;
    showSlides(slideIndex);

    function showSlides(n) {
      const slides = document.querySelectorAll('.slideshow img');
      const dots = document.querySelectorAll('.dot');

      if (n > slides.length) { slideIndex = 1 } // Reset to first slide
      if (n < 1) { slideIndex = slides.length } // Go to last slide

      slides.forEach(slide => slide.classList.remove('active'));
      dots.forEach(dot => dot.classList.remove('active'));

      slides[slideIndex - 1].classList.add('active');
      dots[slideIndex - 1].classList.add('active');
    }

    function currentSlide(n) {
      showSlides(slideIndex = n);
    }

    // Optional: Autoplay functionality (uncomment to enable)
    // setInterval(showSlides, 3000); // Change 3000 to adjust transition interval (in milliseconds)
  </script>
</body>
</html>
